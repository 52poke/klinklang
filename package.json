{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "eslint . --fix",
    "lint": "eslint .",
    "start": "pnpm run -r --parallel start",
    "serve": "pnpm --filter @mudkipme/klinklang-server run --stream serve",
    "build": "pnpm run -r build"
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "eslint-config-love": "^145.0.0",
    "eslint-plugin-react-hooks": "^7.0.1",
    "typescript": "^5.9.3"
  }
}
